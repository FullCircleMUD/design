# Room Architecture

> Technical design document for the room system. For exit architecture and traversal mechanics, see [EXIT_ARCHITECTURE.md](EXIT_ARCHITECTURE.md). For vertical movement and height systems, see [VERTICAL_MOVEMENT.md](VERTICAL_MOVEMENT.md). For procedural dungeon rooms, see [PROCEDURAL_DUNGEONS.md](PROCEDURAL_DUNGEONS.md). For world building and zone design, see [WORLD.md](WORLD.md). For economic systems in rooms, see [ECONOMY.md](ECONOMY.md).

## Design Philosophy

Rooms are the environment engine. They define what the player sees, what they can do, and what rules apply. Every room in the game — from a tavern to an underwater cave to a procedurally generated dungeon — shares a common foundation through `RoomBase`. Specialised behaviors (banking, crafting, harvesting, combat encounters) are added by extending `RoomBase` and injecting room-specific CommandSets.

The room display is modular: each section (name, description, exits, objects, characters, header, footer) is generated by an overridable hook. Subclasses customize display by overriding specific hooks without touching the assembly logic.

## Class Hierarchy

```
DefaultRoom (Evennia)
├── RoomBase (+ QuestTagMixin, FungibleInventoryMixin)
│   ├── RoomBank              — banking commands, safe zone
│   ├── RoomCrafting          — skill-based NFT item crafting
│   ├── RoomProcessing        — resource refinement (mill, smelt, etc.)
│   ├── RoomHarvesting        — wilderness resource gathering
│   ├── RoomInn               — safe zone, quest defeat recovery
│   ├── RoomCemetery          — respawn binding point
│   ├── RoomPostOffice        — mail system
│   ├── RoomGateway           — zone connector (travel/explore)
│   ├── RoomStable            — pet stabling (bank/unbank pattern, large-animal exception)
│   ├── DungeonRoom           — procedural dungeon instance room
│   ├── PressurePlateRoom     — trap room (+ TrapMixin)
│   └── RoomPurgatory         — SYSTEM: death staging, timed release
│
└── RoomRecycleBin            — SYSTEM: orphaned object cleanup (NOT RoomBase)

Limbo (#2)                    — SYSTEM: Evennia default, global fallback
```

## RoomBase

**File:** `typeclasses/terrain/rooms/room_base.py`
**Inherits:** `QuestTagMixin, FungibleInventoryMixin, DefaultRoom`

The foundation for all game rooms. Provides environment systems (lighting, weather, terrain), display rendering, visibility filtering, vertical position tracking, and quest integration.

### Environment Attributes

| Attribute | Default | Purpose |
|---|---|---|
| `allow_combat` | True | Combat enabled in this room |
| `allow_pvp` | False | PvP enabled |
| `allow_death` | True | True death or defeat mode |
| `max_height` | 1 | Maximum flying level (0 = no flying, indoor) |
| `max_depth` | 0 | Maximum swimming depth (0 = no water) |
| `details` | {} | Dict of keyword → description for `look <keyword>` |
| `natural_light` | None | Override lighting (None = derive from terrain) |
| `always_lit` | False | Permanently lit (torches, magical light) |
| `sheltered` | None | Override weather exposure (None = derive from terrain) |

### Zone / District / Terrain Tagging

Every room can be tagged with spatial metadata using Evennia's tag system:

```python
room.set_zone("millholm")           # category="zone"
room.set_district("millholm_town")  # category="district"
room.set_terrain("urban")           # category="terrain"
```

Each category holds one tag at a time — setting a new value replaces the old one.

**Terrain types** (from `enums/terrain_type.py`): URBAN, RURAL, FOREST, MOUNTAIN, DESERT, SWAMP, COASTAL, UNDERGROUND, DUNGEON, WATER, ARCTIC, PLAINS.

Terrain drives three derived properties:

| Property | Logic | Effect |
|---|---|---|
| `has_natural_light` | UNDERGROUND/DUNGEON = False, else True | Dark rooms at night |
| `is_subterranean` | UNDERGROUND/DUNGEON = True | No weather at all |
| `is_sheltered` | URBAN = True, else False | Muffled weather sounds |
| `is_weather_exposed` | Not subterranean AND not sheltered | Full weather effects |

All derivations can be overridden via explicit `natural_light`, `always_lit`, or `sheltered` attributes.

### Lighting System

A room is dark when ALL of these are true:
- NOT `always_lit`
- NOT (has natural light AND it's daytime)
- NOT (lit fixture in room — e.g. lamppost, sconce)
- NOT (looker carries a lit light source — torch, lantern)
- NOT (looker has DARKVISION condition)

Dark rooms show "Unknown" as the room name and "It is pitch black. You can't see a thing." as the description. No exits, objects, or characters are shown.

See also: `LitFixture`, `TorchNFTItem`, `LanternNFTItem` for light source objects.

### Weather Integration

Rooms that are weather-exposed or sheltered display a weather description line appended to the room description. The weather state comes from `WeatherService` keyed by the room's zone tag.

- **Subterranean:** No weather line.
- **Sheltered:** Muffled indoor weather (e.g. "You hear the patter of rain against the roof.").
- **Exposed:** Full weather description (e.g. "Rain falls steadily, soaking your clothes.").

### Quest Integration

All rooms inherit `QuestTagMixin`:

```python
room.quest_tags = ["rat_cellar", "warrior_initiation"]
```

When a character enters a room with quest tags, `at_object_receive()` fires an `"enter_room"` event to the character's quest handler. The handler filters to quests matching the room's tags.

### Mob Arrival Notification

`at_object_receive()` also notifies all mobs in the room via `at_new_arrival(moved_obj)` when a character enters. This is how aggressive mobs detect and attack players on entry.

## Room Display System

The display is assembled by `return_appearance()` from modular hooks. Empty sections are suppressed. Subclasses override specific hooks without touching the assembly.

### Display Sections (in order)

| Section | Hook | Color | Notes |
|---|---|---|---|
| Header | `get_display_header()` | — | Default empty. Subclass hook. |
| Room name | `get_display_name()` | `\|c` cyan | Plus vertical suffix: (Flying), (Swimming), (Underwater) |
| Description | `get_display_desc()` | — | Respects brief mode. Includes weather line. |
| Auto-exits | `get_display_exits()` | `\|c` cyan | Compact `[ Exits: n s e w ]`. Filtered. |
| Objects | `get_display_things()` | `\|g` green | Ground descriptions + grouped bare items + fungibles |
| Characters | `get_display_characters()` | `\|y` yellow | Room descriptions + visibility tags |
| Footer | `get_display_footer()` | — | Default empty. Subclass hook (e.g. DungeonRoom "path blocked"). |

### Exit Display

Compact CircleMUD-style: `[ Exits: n e s w ]`

- Uses direction abbreviations from the exit's `direction` attribute
- Non-directional exits show their full key
- Sorted by canonical compass order (N, E, S, W, NE, NW, SE, SW, U, D, IN, OUT)
- **Filtered:** closed doors hidden, hidden/invisible exits hidden
- Only shown when `looker.auto_exits = True`
- Height-gated exits filtered by looker's vertical position (see [VERTICAL_MOVEMENT.md](VERTICAL_MOVEMENT.md))

### Character Display

One character per line with descriptive sentences:
- NPCs use `room_description` if set (e.g. "Rowan stands behind the bar, polishing a tankard.")
- PCs use position-based templates (e.g. "Bob is resting here.", "Sally stands here.")
- Visibility tags appended: `(invisible)`, `(hidden)` — only for lookers who can detect

### Object Display

Two tiers:
- Objects with `ground_description` attribute shown as full sentences (one per line)
- Bare items grouped by name and counted (e.g. "two corpses of cellar rats")
- Fungibles (gold, resources) appended if present in room

### Darkness Override

When `is_dark(looker)` is True, the entire display is replaced:
- Name: "Unknown"
- Description: "It is pitch black. You can't see a thing."
- No exits, objects, or characters shown
- Header and footer still rendered (for subclass hooks that should work in darkness)

### Vertical Position Display

The room name gets a suffix based on `looker.room_vertical_position`:
- Height 0 in water room → "(Swimming)"
- Height < 0 → "(Underwater)"
- Height > 0 → "(Flying)"

Description gets a prefix:
- Underwater: "Swimming underwater you can dimly perceive above you:"
- Flying: "Flying you can see below you:"

RoomBase has a `vert_descriptions` stub (currently unused) for per-height-level description overrides. See [VERTICAL_MOVEMENT.md](VERTICAL_MOVEMENT.md) for the planned enhancement.

## Visibility Filtering

### msg_contents Override

`RoomBase.msg_contents()` filters broadcast messages based on the sender's visibility:

- **HIDDEN sender:** Message suppressed for all recipients except those with true sight or holy sight (MASTER+)
- **INVISIBLE sender:** Message suppressed for recipients without DETECT_INVIS

`from_obj=caller` is **required** on all `msg_contents` calls for filtering to work. Without it, messages bypass filtering entirely.

### msg_contents_with_invis_alt

For actions where invisible actors produce observable side-effects (tools moving, crafting sounds):

```python
room.msg_contents_with_invis_alt(
    "Bob begins crafting.",              # DETECT_INVIS recipients see this
    "Tools seem to move on their own.",  # everyone else sees this
    from_obj=caller,
)
```

## Specialised Room Types

### RoomBank

**File:** `typeclasses/terrain/rooms/room_bank.py`

Safe zone (no combat/death). Injects `CmdSetBank` for balance, deposit, withdraw commands. Shows welcome message on entry.

### RoomCrafting

**File:** `typeclasses/terrain/rooms/room_crafting.py`

Skill-based NFT item crafting. Each room has a `crafting_type` (smithy, woodshop, tailor, apothecary, jeweller, wizard's workshop) and `mastery_level` that gates recipe complexity. Injects `CmdSetCrafting` for craft, available, repair commands.

### RoomProcessing

**File:** `typeclasses/terrain/rooms/room_processing.py`

Resource refinement (wheat → flour, ore → ingots). No skill required. Supports multi-recipe rooms (smelter handles multiple ore types + alloys). Each recipe can override the default gold cost. Injects `CmdSetProcessing` for process, rates commands.

### RoomHarvesting

**File:** `typeclasses/terrain/rooms/room_harvesting.py`

Wilderness resource gathering. Tracks `resource_count` (current) and `max_resource_count` (cap). `ResourceSpawnService` replenishes hourly based on economy data. Description changes dynamically based on abundance (abundant/scarce/depleted). `spawn_rate_weight` (1-5) controls relative spawn allocation. Injects `CmdSetHarvesting`.

### RoomInn

**File:** `typeclasses/terrain/rooms/room_inn.py`

Safe zone. Injects `CmdSetInn` for stew, ale, menu commands. Quest-integrated: heals defeated rat cellar quest players on arrival (Rowan the bartender narrative).

### RoomCemetery

**File:** `typeclasses/terrain/rooms/room_cemetery.py`

Respawn binding point. Players pay `bind_cost` gold to set this room as their death respawn location. Injects `CmdSetCemetery` for the bind command.

### RoomPostOffice

**File:** `typeclasses/terrain/rooms/room_postoffice.py`

Mail system hub. Shows unread mail count on arrival. Injects `CmdSetPostOffice` for mail commands. Safe zone.

### RoomGateway

**File:** `typeclasses/terrain/rooms/room_gateway.py`

Zone connector for sea/overland travel. Stores a `destinations` list of dicts defining routes with conditions (food cost, gold cost, level requirement, boat level). Hidden destinations can be discovered via the `explore` command (uses bread as fuel for exploration rolls). Injects `CmdSetGateway` for travel and explore commands.

### RoomStable

**File:** `typeclasses/terrain/rooms/room_stable.py`

Pet stabling location. Tagged `stable` (category `system`) so the size system makes a large-animal exception — players can bring horses, mules, and other large pets indoors here. Injects `CmdSetStable` for `stable <pet>`, `retrieve <pet>`, and `stabled` (list) commands. Stabling moves the pet object into the owner's `AccountBank` via the same `at_post_move` → `NFTService.bank()` dispatch used for item banking; retrieval is the inverse. Stable fees combine a base rent plus per-pet feed and heal costs. See [PETS_AND_MOUNTS.md](PETS_AND_MOUNTS.md) § Stabling for the full lifecycle.

### DungeonRoom

**File:** `typeclasses/terrain/rooms/dungeon/dungeon_room.py`

Procedurally generated room inside a dungeon instance. Tracks `xy_coords` (grid position), `dungeon_instance_id`, and `is_boss_room` flag. The `not_clear` tag (category `dungeon_room`) gates forward exit traversal — shows "The path forward is blocked!" in footer. See [PROCEDURAL_DUNGEONS.md](PROCEDURAL_DUNGEONS.md).

### PressurePlateRoom

**File:** `typeclasses/terrain/rooms/room_pressure_plate.py`
**Inherits:** `TrapMixin, RoomBase`

Trap room. First character to enter is frozen in place. Attempting to leave triggers an explosion (AoE damage to all occupants). Can be disarmed via the `disarm` command. TrapMixin provides trap detection, triggering, and disarming mechanics.

## System Rooms

Infrastructure rooms that support game mechanics. Not part of any zone — created once during world setup and never deleted.

### Limbo

**Evennia default room** (`#2`, `settings.DEFAULT_HOME`)

Global fallback location. Characters with no home end up here. Zone cleanup evacuates players to Limbo before deleting rooms. New characters start here before entering the tutorial or game world. Contains a one-way exit down to The Harvest Moon (game entry point, created by `deploy_world` with `one_way_limbo=True`).

### RoomPurgatory

**File:** `typeclasses/terrain/rooms/room_purgatory.py`

Death staging area. Characters arrive here on death via `die()`, auto-released after 60 seconds to their bound cemetery (or Limbo if unbound). Early release costs 50 gold. Injects `CmdSetPurgatory` for the release command. Created once in `deploy_world()`.

### RoomRecycleBin

**File:** `typeclasses/terrain/rooms/room_recycle_bin.py`
**Inherits:** `DefaultRoom` (NOT RoomBase)

Hidden infrastructure room for orphaned object cleanup. No exits, no player interaction. Handles three cases:

- **Characters** accidentally moved here are teleported to their home (or DEFAULT_HOME).
- **NFT items** are despawned from the blockchain mirror DB and deleted.
- **All other objects** (WorldItems, fixtures, corpses, etc.) are deleted with a log message.

This is the catch-all safety net — anything that loses its location during zone cleanup, room deletion, or container destruction ends up here and is cleaned up automatically. Created once in `deploy_world()`. All NFT items use this room as their `home` so orphaned NFTs are always caught.

## CmdSet Injection Pattern

Every specialised room injects a room-specific CmdSet in `at_object_creation()`:

```python
def at_object_creation(self):
    super().at_object_creation()
    self.cmdset.add(CmdSetBank, persistent=True)
```

Evennia merges room CmdSets with character CmdSets dynamically. Commands are available when the character is in the room and disappear when they leave. No manual cleanup needed.

## FungibleInventoryMixin

All rooms inherit `FungibleInventoryMixin` from `RoomBase`, giving them the ability to hold gold and resources. This is used for:

- **Harvesting rooms:** `resource_count` tracks available resources
- **Dungeon rooms:** hold dropped gold/resources, returned to RESERVE on instance collapse
- **World rooms:** items dropped by players or spawned by scripts

Rooms are classified as `"WORLD"` for blockchain service dispatch — fungible operations use the vault wallet address, not a player wallet.

## Key Implementation Rules

1. **Always extend RoomBase** for game rooms — never bare `DefaultRoom` (except RoomRecycleBin which intentionally avoids game systems).
2. **CmdSets are injected once** in `at_object_creation()` with `persistent=True`. They survive server restarts.
3. **Override display hooks, not return_appearance** — customize rooms by overriding `get_display_header/footer/desc`, not by rewriting the assembly logic.
4. **`from_obj=caller` is mandatory** on all `msg_contents` calls for HIDDEN/INVISIBLE filtering to work.
5. **Terrain drives defaults** — set terrain once, lighting and weather exposure derive automatically. Only override `natural_light`, `always_lit`, or `sheltered` when the default derivation is wrong.
6. **Quest tags are lists** — a room can trigger multiple quests on entry.
7. **Safe zones** set `allow_combat=False, allow_pvp=False, allow_death=False` — this is the standard pattern for banks, inns, temples, etc.
8. **Room/object lookup: always filter by district tag.** Evennia's `db_key` is NOT globally unique — it's also the player-visible room name. Multiple zones will have rooms with the same key (e.g. "The Vault"). Always scope `ObjectDB.objects.filter()` with a district or zone tag, or search within a known room's `.contents`. See CLAUDE.md § "Room/Object Lookup Convention" for full patterns.

## Test Coverage

Room display, lighting, details, and exit formatting are tested across:
- `tests/typeclass_tests/test_room_display.py` — room rendering, brief mode, darkness
- `tests/command_tests/test_cmd_look_details.py` — room details system
- `tests/command_tests/test_cmd_exits.py` — verbose exit display
- `tests/dungeon_tests/` — dungeon room integration
